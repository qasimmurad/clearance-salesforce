streamlit>=1.30
pandas>=2.0
altair>=5.0

# Optional. The app runs fully without this — the drafting engine is
# deterministic by default, which is why the demo never fails live.
# Uncomment to let Clearance draft with a real model instead
# (also needs ANTHROPIC_API_KEY set in the environment).
# anthropic>=0.40
